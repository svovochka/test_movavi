{% extends 'AppBundle:admin:layout.html.twig' %}

{% block title %}Create category{% endblock %}

{% block content %}

    <h1>Create category</h1>

    <p><a href="{{ path('admin_categories') }}">Back</a></p>

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
{% extends 'AppBundle:admin:layout.html.twig' %}

{% block title %}Create category{% endblock %}

{% block content %}

    <h1>Create category</h1>

    <div>
        <a class="btn btn-default" href="{{ path('admin_categories') }}"><i
                    class="glyphicon glyphicon-chevron-left"></i> Back to list</a>
    </div>

    <section class="vertical-indent">
        <div>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </section>

{% endblock %}